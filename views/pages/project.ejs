<%- include('../commons/head', {title :project.name}) %>

<body>
    <%- include('../commons/navbar', {userinfo: userinfo}) %>
    <div class="p-3">
        <h1><%= project.name %></h1>
        <h3><%= project.description %></h3>
        <div class="p-2" style="margin-left: -2rem;">
            <% for (let issue of rootIssueList){ %>
                <%- include('../partials/issue', {issue: issue}) %>
            <% } %>
        </div>
    </div>
</body>

<%- include('../commons/foot') %>